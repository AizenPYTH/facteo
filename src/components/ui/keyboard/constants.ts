/**
 * Constantes clavier — source unique pour toute l’app.
 *
 * La KeyboardToolbar native n’est plus montée (crash iOS au lancement).
 * Les pieds d’action se calent donc directement au-dessus du clavier.
 */
export const KEYBOARD_TOOLBAR_HEIGHT = 0;

/** Marge minimale au-dessus du clavier / de la toolbar (pas l’home indicator). */
export const KEYBOARD_OPENED_FOOTER_PADDING = 8;
